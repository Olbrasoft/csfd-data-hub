/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // We serve data files from /public/data — no special config needed.
  }
};

export default nextConfig;
